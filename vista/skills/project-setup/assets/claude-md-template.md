# {PROJECT_NAME}

Use the project skill before starting any task.

## Spec Management

When requirements change during implementation, use `/vista:specs <feature>` to update specs with tracked changes. All spec-related commits use the `spec(feature/topic): description` prefix. Reference requirement IDs in code with `# Implements {ID}: description`.
